trigger:
- main

name: Deploy Bicep files

variables:
  vmImageName: 'windows-latest'

pool:
  vmImage: $(vmImageName)

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: | 
      Write-Host "Check Username"

      $host = $env:USERNAME
      Write-host $host
      Get-module -listavaible